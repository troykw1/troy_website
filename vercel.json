{
  "version": 2,
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node"
    }
  ],
"routes": [
    { "handle": "filesystem" },
    { "src": "/Contact/(.*)", "dest": "/Contact/$1" },
    { "src": "/(.*)", "dest": "src/server.ts" }
  ]
}