
Name and Host: oregon-postgres.render.com

Username: admin

Maintenance DB: 
--> test_z25z

CREATE TABLE contacts (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
	message VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL
);

INSERT INTO contact (name, message, email)
VALUES
    ('John Rather', 'Hello','john.rather@example.com'),
    ('Jane Smith', 'Hi','jane.smith@example.com');

SELECT * FROM contact;

CREATE TABLE contact (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
	message VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL
);


CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL
);

\l  Lists all available databases on the server 
\c <database_name>: Switches to the specified database 

\c contacts

Now we seed a dummy data into the table
---> I inserted this into the commnad line 

INSERT INTO users (name, email)
VALUES
    ('John Rather', 'john.rather@example.com'),
    ('Jane Smith', 'jane.smith@example.com');

SELECT * FROM users;